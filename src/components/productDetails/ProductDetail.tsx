// import { useParams } from "react-router-dom";

export const ProductDetail = (): JSX.Element => {
  // const { id } = useParams();

  // ? el valor del product se va asignar a traves de un store y un metodo en reduxP
  // const product;

  return (
    <div className="w-96 h-96 p-28 bg-white border-2 border-black justify-center items-center inline-flex">
      {/*<img src={product.thumbnail} alt={product.title} />
      <div>
        <h2>{product.title}</h2>
        <p>Price: {product.price}</p>
        <p>Quantity: {product.quantity}</p>
        <p>Total: {product.total}</p>
        <p>Discount: {product.discountPercentage}%</p>
        <p>Discounted Price: {product.discountedPrice}</p>
      </div>*/}
    </div>
  );
};
